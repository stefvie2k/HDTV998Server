These are bash scripts to be used in conjuction with OpenCaster 3.2.2.

First, you need to run the atsc.py within the psip directory and then alter the cast.sh to fit your need.
Best to start with the "test" broadcasting which creates a test pattern using sox and ffmpeg.

In all cases, you need to change where the tscbrmuxer sends the stream.
